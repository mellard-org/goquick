buildPack: none
pipelineConfig:
  pipelines:
    pullRequest:
      pipeline:
        agent:
          image: mellardc/potato
        stages:
          - name: build
            steps:
              - name: build-and-push-image
                args: ['--dockerfile=Dockerfile','--destination=tfjxlegibledog.azurecr.io/potato']
